<?xml version="1.0" encoding="UTF-8"?>
<features name="${project.artifactId}-${project.version}">
    <feature name="dm-bundle" version="${project.version}">
        <bundle>mvn:${project.groupId}/dm-bundle/${project.version}</bundle>
    </feature>
</features>
